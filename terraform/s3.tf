# ============================================================
# S3 — REMOVED
# AWS Academy lab policy (voc-cancel-cred) explicitly denies
# s3:CreateBucket and s3:PutBucketVersioning on this account.
# Terraform state is stored locally (local backend) instead of
# in an S3 bucket.
# ============================================================
